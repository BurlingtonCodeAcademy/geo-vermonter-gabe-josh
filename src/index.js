import React from "react";
import ReactDOM from "react-dom";
import App from './App'

//Renders App component to the React DOM
ReactDOM.render(
  <div>
    <App />
  </div>,
  document.getElementById("root")
);